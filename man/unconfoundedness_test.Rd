% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unconfoundedness_test.R
\name{unconfoundedness_test}
\alias{unconfoundedness_test}
\title{Test unconfoundedness with comprehensive diagnostics and multiple estimators}
\usage{
unconfoundedness_test(
  data_rct,
  data_obs,
  formula,
  estimator = c("aipw", "ipw", "tmle", "gcomp", "matching"),
  effect_measure = c("rd", "rr", "or"),
  stabilize = TRUE,
  trim = c(0.01, 0.99),
  family_y = c("gaussian", "binomial"),
  transport = c("none", "rct_to_obs", "auto"),
  auto_method = c("both", "ks", "energy"),
  auto_alpha = 0.01,
  auto_energy_R = 199L,
  inference_method = c("bootstrap", "analytical", "robust"),
  B = 1000L,
  alpha = 0.05,
  parallel = FALSE,
  n_cores = NULL,
  seed = NULL,
  validate = TRUE
)
}
\arguments{
\item{data_rct, data_obs}{data.frame with same Y/A definitions and covariates}

\item{formula}{model formula \code{Y ~ A + X1 + X2}. A must be binary and the first RHS term}

\item{estimator}{character: "aipw" (default), "ipw", "tmle", "gcomp", "matching"}

\item{effect_measure}{character: "rd" (risk difference), "rr" (risk ratio), "or" (odds ratio)}

\item{stabilize}{logical; stabilized IPW (default TRUE)}

\item{trim}{length-2 numeric in [0,1] for weight trimming; NULL disables}

\item{family_y}{"gaussian" or "binomial"}

\item{transport}{"none", "rct_to_obs", or "auto" (default "none")}

\item{auto_method}{"both", "ks", "energy" for shift detection}

\item{auto_alpha}{significance level for shift tests (default 0.01)}

\item{auto_energy_R}{permutations for energy test (default 199)}

\item{inference_method}{"bootstrap", "analytical", "robust"}

\item{B}{bootstrap replicates (default 1000)}

\item{alpha}{CI level (default 0.05 for 95\% CI)}

\item{parallel}{logical; use parallel processing for bootstrap}

\item{n_cores}{integer; cores for parallel processing (NULL = auto-detect)}

\item{seed}{RNG seed or NULL}

\item{validate}{logical; perform extensive input validation}
}
\value{
enhanced unconf_test object with comprehensive diagnostics
}
\description{
Compare a marginal treatment effect in an RCT-like dataset with the same
estimand in an observational dataset. Enhanced version supports:
\itemize{
\item Estimators: IPW, AIPW, TMLE, G-computation, matching
\item Effect measures: risk difference, risk ratio, odds ratio
\item Inference: bootstrap CI, analytical SE, robust SE
\item Transport weighting with auto-detection
\item Comprehensive diagnostics and visualization
\item Sensitivity analysis tools
}
}
\examples{
\dontrun{
# Enhanced example with multiple estimators
set.seed(42)
data_rct <- generate_rct_data(n = 800)
data_obs <- generate_obs_data(n = 2000, confounding = TRUE)

# Comprehensive test
result <- unconfoundedness_test(
  data_rct, data_obs,
  Y ~ A + X1 + X2,
  estimator = "aipw",
  effect_measure = "rd",
  transport = "auto",
  inference_method = "bootstrap",
  B = 1000,
  parallel = TRUE,
  validate = TRUE
)

print(result)
plot(result)
summary(result)
}
}
\seealso{
\code{\link[=generate_rct_data]{generate_rct_data()}} and \code{\link[=generate_obs_data]{generate_obs_data()}} for simulating input data,
\code{\link[=batch_analysis]{batch_analysis()}} for running multiple analyses,
\code{\link[=print.unconf_test]{print.unconf_test()}}, \code{\link[=summary.unconf_test]{summary.unconf_test()}}, and \code{\link[=plot.unconf_test]{plot.unconf_test()}}
for inspecting results.
}
