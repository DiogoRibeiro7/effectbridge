% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unconfoundedness_test.R
\name{extract_covariates}
\alias{extract_covariates}
\title{Extract covariate names from a model formula}
\usage{
extract_covariates(formula, data, treat = "A")
}
\arguments{
\item{formula}{stats::formula like \code{Y ~ A + X1 + X2}}

\item{data}{data.frame used to validate which names actually exist}

\item{treat}{character(1) name of the treatment variable (default "A")}
}
\value{
character() vector of covariate names present in \code{data}
}
\description{
Given a formula like \code{Y ~ A + X1 + X2}, return the covariate names
to be used in the propensity model: all symbols on the RHS except \code{treat}.
Works with interactions (X1:X2), functions (log(X1)), and backticked names.
If a variable isn't a column in \code{data}, it's dropped.
}
\examples{
df <- data.frame(Y=0, A=0, X1=1, X2=2, X3=3)
extract_covariates(Y ~ A + X1 + X2, df)          # c("X1","X2")
extract_covariates(Y ~ A + log(X1) + X2:X3, df)  # c("X1","X2","X3")

}
\keyword{internal}
